# config/preprod.py

from .base import *

# ======================================================
# PRODUCCIÓN / PREPRODUCCIÓN
# ======================================================

DEBUG = False

# ✅ Ajustado para este servidor (staging por VPN / túnel SSH)
ALLOWED_HOSTS = [
    "127.0.0.1",
    "localhost",
    "172.17.40.137",
]

# ✅ Para staging sin dominio podés dejar vacío.
# Cuando tengas dominio/https, agregás el/los orígenes reales.
CSRF_TRUSTED_ORIGINS = []

# Si más adelante usás Nginx + HTTPS detrás de proxy, esto queda bien.
SECURE_PROXY_SSL_HEADER = ("HTTP_X_FORWARDED_PROTO", "https")
USE_X_FORWARDED_HOST = True


# ======================================================
# BASE DE DATOS (PostgreSQL)
# ======================================================

DATABASES = {
    "default": {
        "ENGINE": "django.db.backends.postgresql",
        "NAME": "nueva_web_dav_preprod",
        "USER": "nueva_web_dav_user",
        "PASSWORD": "Dav30180!",
        "HOST": "127.0.0.1",
        "PORT": "5432",
    }
}


# ======================================================
# EMAIL (SMTP REAL)
# ======================================================

EMAIL_BACKEND = "django.core.mail.backends.smtp.EmailBackend"
EMAIL_HOST = "smtp.gmail.com"
EMAIL_PORT = 587
EMAIL_USE_TLS = True

# ✅ Credenciales fuera de Git (si existe config/secrets.py)
from .secrets import (
    EMAIL_HOST_USER,
    EMAIL_HOST_PASSWORD,
    DEFAULT_FROM_EMAIL,
)
